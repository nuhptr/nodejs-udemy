const summarizeUser = (userName, userAge, userHasHobby) => {
  return (
    "name is " +
    userName +
    ", age is " +
    userAge +
    ", and the user has hobbies: " +
    userHasHobby
  );
};

const addOnes = (a) => a + 1;
